\renewcommand*{\cite}{\errmessage{Use \autocite!}}
\renewcommand*{\eqref}{\errmessage{Use \cref!}}
\renewcommand*{\autoref}{\errmessage{Use \cref!}}

% allow the use of an 'axiom' counter
\crefformat{axiom}{axiom~#2#1#3}
\crefrangeformat{axiom}{axioms~#3#1#4 to~#5#2#6}
\crefmultiformat{axiom}{axioms~#2#1#3}{ and~#2#1#3}{, #2#1#3}{ and~#2#1#3}
% allow the use of an 'case' counter (for proof-by-cases)
\crefformat{case}{case~#2#1#3}
\crefrangeformat{case}{cases~#3#1#4 to~#5#2#6}
\crefmultiformat{case}{cases~#2#1#3}{ and~#2#1#3}{, #2#1#3}{ and~#2#1#3}
% change format eq. (..) to just (..)
\crefformat{equation}{(#2#1#3)}
\crefrangeformat{equation}{(#3#1#4) to~(#5#2#6)}
\crefmultiformat{equation}{(#2#1#3)}{ and~(#2#1#3)}{, (#2#1#3)}{ and~(#2#1#3)}
% allow the use of an 'rq' counter (for research questions)
\crefformat{rq}{#2#1#3}
\crefrangeformat{rq}{#3#1#4 to~#5#2#6}
\crefmultiformat{rq}{#2#1#3}{ and~#2#1#3}{, #2#1#3}{ and~#2#1#3}
% also show only the item name for research questions
\crefalias{rq}{equation}

% csquotes: easy quotes
\MakeOuterQuote{"}

% theorem
\declaretheorem[parent=chapter, title=Corollary]{corollary}
\declaretheorem[parent=chapter, title=Definition]{definition}
\declaretheorem[parent=chapter, title=Lemma]{lemma}
\declaretheorem[parent=chapter, title=Proposition]{proposition}
\declaretheorem[parent=chapter, title=Theorem]{theorem}
\declaretheorem[title=Proof (Sketch), numbered=no, style=remark]{proof-sketch}

% exclude subsections from toc
\setcounter{tocdepth}{1}

% tikzmake arrow longer
\tikzset{> = { Latex[length = 2mm] }}

% algorithm2e: typeset comments as \text not \texttt
\SetCommentSty{text}

% do not emphasize foreign words
\renewcommand{\foreignabbrfont}{}

% make loa, lof, lot sections
\makeatletter
\newcommand{\listalgorithmname}{List of Algorithms}
\renewcommand\listofalgorithms{\section*{\listalgorithmname}\@starttoc{loa}}
\renewcommand\listoffigures{\section*{\listfigurename}\@starttoc{lof}}
\renewcommand\listoftables{\section*{\listtablename}\@starttoc{lot}}
\makeatother

% do not place algorithm line numbers outside the box
\setlength{\algomargin}{1.5em}

% caption: center single-line captions, flush multi-line captions to the left
\captionsetup{singlelinecheck=on}

% siunitx setup, in particular boldface in tables
\newrobustcmd{\best}{\DeclareFontSeriesDefault[rm]{bf}{b}\bfseries}
\sisetup{
    separate-uncertainty=true,  % write uncertainty as 10 \pm 2, not 10(2)
    reset-text-series=false,
    text-series-to-math=true,
    mode=text
}

% sometimes, you just can't do anything about an overfull hbox...
\newcommand{\ignorehbadness}[1]{\begingroup \hbadness=100000 \hfuzz=\maxdimen #1 \endgroup}
